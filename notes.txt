Higher Order function
-----------------------
   -> A function which takes another function as arguments or return a function is known as higher order function

Example:
---------
function x(){
   console.log("x printed")
}

function y(x){
   x()
}

y(x)

-> reusability ,modularity 

-> check the image ,we get to know how we can prevent reuse of code and how we can refactor


Prototype
----------
   -> it is just an object when we create array or object then the proto object is getting attached to the array or object which we define ,the javascript itself
       will add it
   



Prototype Inheritance
----------------------
   -> refer the prototype image
   -> this is a sort of scope chain 


example :
   Function.prototype.myBind=function (){
      console.log("start")
   }

   function b(){

   }
   b.mybind()


this function b will have access to my bind function because we have defined it  in the prototype scope 
The settimeout has trust  issues ,because even if we mention the call back function to ruun after 5 sec until the call stack is empty the settimeout function wont run


-> example : blocking the main thread and setting the settimeout

console.log("start")


setTimeOut(function(){
   console.log("console printed")
},5000)


let startDate=new Date().getTime()
let endDate=startDate

while(endDate <startDate+1000){
   endDate=new Date().getTime()
}

console.log("end")

result:
   start
   end
   console printed


-> here i have blocked the main thread for 10 seconds to check the behaviour of settimeout
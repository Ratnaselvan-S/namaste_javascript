this in global space
--------------------
   -> this in browser will always point to window object ,we know javascript can run on any where if we have javascript run time ,like bulb,fridge ,mobile etc so i therethe 
      keyword this can be different

   -> in strict mode the behaviour is different

   -> strict mode differnce (To use strict mode just on javascript top put this keyword "use strict")
         console.log(this);

         function x(){
            console.log(this);
         }

         result:
            window object 
            undefined
   -> non strict mode differnce

      console.log(this);

         function x(){
            console.log(this);
         }

         result:
            window object 
            window object

this inside non strict mode - (this substitution)
-----------------------------------------
   -> if when we log this keyword on anywhere and if it is undefined or null then it will be replaced with global object only in non strict mode

this keyword value depends on how the fucntion is called 
---------------------------------------------------------
   under strict mode
   -----------------
    ->console.log(this);

         function x(){
            console.log(this);
         }
      x()
         result:
            window object 
            undefined
--------------------------------
   ->console.log(this);

            function x(){
               console.log(this);
            }
         window.x()
            result:
               window object 
               window object

      under non strict mode 
      ----------------------
      -> result:
            window object
            window object
            window object


method
----------
   -> function that is part of the object is known as method


this keyword inside object
------------------------------
   const obj={
      a:10
      x:function (){
         console.log(this)
      }
   }
   obj.x()

   result:
   --------
         {
      "a": 10
         }

whenever we talk about this keyword three impotant topics
----------------------------------------------------------
   -> call 
   -> apply
   -> bind



 call 
--------

   const obj={
         a:10,
         x:function (){
            console.log(this.a)
         }
      }

   const obj2={
      a:"Hello ratna"

   }

   obj.x.call(obj2)

result:
--------
   Hello ratna

this  keyword inside arrow function
------------------------------------
   -> inside arrow function it will behave differently ,it wont reference the curr obj but it will represent  the value of it enclosing lexical context

    const obj={
         a:10,
         x: ()=>{
            console.log(this)
         }
      }
result:
--------
   window object because the object is in global scope
-----------------------------------------------------------------------------------

if i do this 
    const obj={
         a:10,
         x:function  (){
            const z=()=>{
            console.log(this)

            }
            z()
         }
      }

   obj.x()

   result:
      {
         "a": 10
         x:f
      }

because lexical scop of the function is the obj

dom element
--------------

<button onclick="alert(this)">Click me </button>


result :
   in alert this will give object html button element | it means the this keyword in dom refernce to the html element where we invoke it 
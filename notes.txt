so in let when we have let a=10 ,it does not behave like var the let variable is assigned not in global scope but a script object in meory while skiming and 
like var it does not console undefined ,it says character a is not initialized 

example
    console.log(a)
    let a=5; 
    var b=10

-> when the javascript skims the b is allocated undefiined and in global scope of memery but a is on script scope and it is also assigned undefined 
-> when the let a is undefined until initialzation that is the time period between this is known as temporal dead zone
-> so whenever we are in temporal deadzone there will be "reference error "


const  type when we reinitalize it then "type error" will come 

syntax error is when we do not follow ecmascriptStandard then the systax error comes 
->Call stack is inside the javascript Engine

->see the image "browser_superPowers.png" ,there are different thing that is avaliable ,if javascript need to access them then comes the web apis

-> the web api is not written in javascript ex-(document.getElementById,console,fetch,settimeout) these are not written in javascript


The browser (Chrome, Firefox, etc.) is built with different subsystems inside the Web API layer:
--------------------------------------------------------------------------------------------------

   ~ Timer subsystem → handles setTimeout, setInterval.

   ~ Networking subsystem → handles fetch, XHR, WebSocket.

   ~ DOM event subsystem → handles addEventListener registrations for click, keydown, etc.

   ~ Other APIs → Geolocation, IndexedDB, etc.


setTimeout
-----------------
console.log("start")

settimeout(function cb(){
    console.log("timer")
},5000)


-> this is basic example of how it executes first console will call the console web api and print start and then the settimeout call the settimeout api and then the call back function is 
    stored in Web API (Timer subsystem) and after timer completed the call back function is pushed to callback queue and event loop will then take the function and put it in 
    call stack 


console.log("start")


dom web api
--------------

document.getElementByid("button").addEventListener("click",function cb(){
    console.log("execute");
})

->same step ,here the global execution context is created and then console is executed and then document web api will store the event in dom event subsystem and then register with click event 
    when the event occures then from dom event subsytem it moves to callback queue and then by event loop if call stack is empty it execute the cb function


call back queue also known as task queue
------------------------------------------

why we need call back queue ?
    ->if no ccall back queue and we take it directly form subsystem then below scenario
    supporse if a click event and set timeout event is both there and if i click the click event continuosly then the click event execute next after next the timer event wont have its chance 
    thus call back queue is there so everything gets its chance

    -> The Callback Queue ensures fair scheduling so that all subsystems’ callbacks (click, timeout, network, etc.) eventually get executed.

-> here all the domweb api,settimeout api,etc all comes under call back queue

Fetch api 
-----------

settimeout(function cb(){
    console.log("timer")
},5000)

fetch("httsp://google.com").then(function cf(){
    console.log("fetch Executed")
})


-> when the settimeout comes the timeout web api is called and then time sussytem the call back function is stored and move on to fetch here fetch api is called and call back function 
    move to network web api and once the data is received it is placed into microtask queue (this is same as call stack but it has higher priority ) so when the event loop check 
    empty call stack ,then it first check microtask queue any function is there and then next to call stack queue

Microtask Queue
----------------
-> all promises and mutation observer comes under micro tast queue


starvation
-----------
    -> when the micro tast queue when execute one call back function and it create another cb function in micro task queue and this keeps on going ,then starvation occurs in 
        call back queue
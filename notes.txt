Create a promise
------------------

 const promise=createCart(cart)

 const cart=["shows","Hello"]

 function createCart(cart){
    const pr=new PRomises(function(resolve,reject){

        if(!validateCart(cart)){
            reject(cart)
        }

        const orderid="12355", //here only the db call is made and may be create an order and return the order id 
        if(orderid){
            resolve(orderid)
        }

    })

    return pr
 }
-> here we are not catching the error type so in browser console there will be red mark and indication of error so we write catch block and do something with the error

-> if in promise chaining we need to chain the promise like .then.then and we return inside the function so the promise chain is succesful (remenber to put the return keyword if
the next then block is corresponding to the previous one)


-> also the catch block is there right it will catch all the error if it occur on any level of then block

-> so consider a senario if one catch block is last then even if one fail then it will go directly to catch block and we do not know which then block goes error
    so for each "then" we can write "catch" block 


    (refer the image for better understanding)
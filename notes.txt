even an empty javascript file when it runs a global state is added to call stack and execution context is still created 

in the global space on empty file javascript engine still creates window object 

and suppose if we have this code 

var x=10;
console.log(window.x)
console.log(this.x)
console.log(x) //this will automatically search for that execution context space and if we define globally then the x will append to global object 